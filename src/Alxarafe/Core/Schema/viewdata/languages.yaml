form-type: form-horizontal
fields:
    id:
        component: input
        type: number
        label: id
        shortlabel: col-id
        placeholder: id
    name:
        component: input
        type: text
        label: name
        shortlabel: col-name
        placeholder: name
        maxlength: 50
    language:
        component: input
        type: text
        label: language-code
        shortlabel: col-language-code
        placeholder: language-code
        pattern: '[a-z]{2}'
        maxlength: '2'
        title: language-code
    variant:
        component: input
        type: text
        label: language-variant
        shortlabel: col-language-variant
        placeholder: language-variant
        pattern: '[A-Z]{2}'
        maxlength: '2'
        title: language-variant
