form-type: form-horizontal
fields:
    id:
        component: input
        type: number
        label: id
        shortlabel: col-id
        placeholder: id
    name:
        component: input
        type: text
        label: name
        shortlabel: col-name
        placeholder: name
        maxlength: 50
    path:
        component: input
        type: text
        label: path
        shortlabel: col-path
        placeholder: path
        maxlength: 100
    enabled:
        component: input
        type: datetime
        label: enabled
        shortlabel: col-enabled
        placeholder: enabled
    inserted_date:
        component: input
        type: datetime
        label: inserted_date
        shortlabel: col-inserted_date
        placeholder: inserted_date
    updated_date:
        component: input
        type: datetime
        label: updated_date
        shortlabel: col-updated_date
        placeholder: updated_date
