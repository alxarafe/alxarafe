{#
 ~ Alxarafe. Development of PHP applications in a flash!
 ~ Copyright (C) 2018-2019 Alxarafe <info@alxarafe.com>
#}
{% extends "index.twig" %}

{% block body %}
    {{ parent() }}
    <div class="container-fluid">
        <form action="{{ constant('BASE_URI') }}/index.php?{{ constant('CALL_CONTROLLER') }}=EditConfig&{{ constant('METHOD_CONTROLLER') }}={{ constant('DEFAULT_METHOD') }}" method='post' accept-charset="utf-8">
            <div class="row">
                <div class="col-xs-12">
                    {% if (message) %}{{ message }}{% endif %}
                </div>
            </div>
            <div class="row">
                <div class="col-xs-12 col-sm-6 col-md-4">
                    <div class="form-group">
                        <label>{{ lang.trans('database-engine') }}</label>
                        <select id="dbEngineName" name="dbEngineName" class="form-control">
                            {% for dbEngine in ctrl.dbEngines %}
                                <option {{ dbEngine==ctrl.dbEngineName ? "selected":"" }}>{{ dbEngine }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
                <div class="col-xs-12 col-sm-6 col-md-4">
                    {% if ctrl.skins|length > 0 %}
                        <div class="form-group">
                            <label>{{ lang.trans('default-skin') }}</label>
                            <select id="skin" name="skin" class="form-control">
                                {% for skin in ctrl.skins %}
                                    <option {{ skin==ctrl.skin ? "selected":"" }}>{{ skin }}</option>
                                {% endfor %}
                            </select>
                        </div>
                    {% endif %}
                </div>
                {% for key,var in ctrl.dbConfig %}
                    <div class="col-xs-12 col-sm-6 col-md-4">
                        <div class="form-group">
                            <label>{{ lang.trans(key) }}</label>
                            <input id="{{ key }}" name="{{ key }}" class="form-control" type="text" value="{{ var }}"/>
                        </div>
                    </div>
                {% endfor %}
                <div class="col-xs-12 col-sm-6 col-md-4">
                    {% if ctrl.languages|length > 0 %}
                        <div class="form-group">
                            <label>{{ lang.trans('default-language') }}</label>
                            <select id="language" name="language" class="form-control">
                                {% for lang in ctrl.languages|keys %}
                                    <option {{ lang==ctrl.language ? "selected":"" }}>{{ lang }}</option>
                                {% endfor %}
                            </select>
                        </div>
                    {% endif %}
                </div>
            </div>
            <div class="row">
                <div class="col-xs-6 col-sm-6 col-md-6">
                    <button name="action" type="submit" class="btn btn-default btn-danger" value="cancel">
                        {{ lang.trans('exit') }}
                    </button>
                </div>
                <div class="col-xs-6 col-sm-6 col-md-6 text-right">
                    <button name="action" type="submit" class="btn btn-success btn-primary" value="clear-cache">
                        <span class="glyphicon glyphicon-erase" aria-hidden="true"></span>
                        {{ lang.trans('clear-cache') }}
                    </button>
                    <button name="action" type="submit" class="btn btn-success btn-primary" value="regenerate-data">
                        <span class="glyphicon glyphicon-play" aria-hidden="true"></span>
                        {{ lang.trans('regenerate-data') }}
                    </button>
                    <button name="action" type="submit" class="btn btn-default btn-primary" value="save">
                        <span class="glyphicon glyphicon-floppy-save" aria-hidden="true"></span>
                        {{ lang.trans('save-changes') }}
                    </button>
                </div>
            </div>
        </form>
    </div>
{% endblock %}
