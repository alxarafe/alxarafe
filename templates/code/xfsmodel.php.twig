<pre>
{#
<?php
/**
 * Alxarafe. Development of PHP applications in a flash!
 * Copyright (C) 2018 Alxarafe <info@alxarafe.com>
 */
#}
namespace Xfs\Models;

use Xfs\Base\XfsTable;

/**
 * Class {{ ctrl.modelName }}
 *
 * @package Xfs\Models
 */
class {{ ctrl.modelName }} extends XfsTable
{
    /**
     * {{ ctrl.modelName }} constructor.
     *
     * @param bool $create
     */
    public function __construct(bool $create = false)
    {
        parent::__construct('{{ ctrl.tableName }}', ['create' => $create]);
    }

    /**
     * Returns the name of the identification field of the record. By default it will be name.
     *
     * @return string
     */
    public function getNameField(): string
    {
        return 'name';
    }
{% if ctrl.modelDeps is not empty %}

    /**
     * Return class dependencies
     */
    public function getDependencies()
    {
        return [
{% for item in ctrl.modelDeps %}
            '{{ item }}',
{% endfor %}
        ];
    }
{% endif %}
}
</pre>